import { CalendarCheck2, CheckCircle2, Clock3, LoaderCircle, Pill, ShieldCheck, Stethoscope } from 'lucide-react'
import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { isStaticDemo } from '../config/runtime'
import { messageFromError } from '../services/api'
import { getMyFollowUps, updateFollowUpStatus, type CareFollowUp, type FollowUpStatus } from '../services/followUps'

const labels: Record<FollowUpStatus, string> = { SCHEDULED: 'Scheduled', CONFIRMED: 'Confirmed', COMPLETED: 'Completed', MISSED: 'Missed' }
const styles: Record<FollowUpStatus, string> = { SCHEDULED: 'bg-blue-50 text-blue-800', CONFIRMED: 'bg-violet-50 text-violet-800', COMPLETED: 'bg-emerald-50 text-emerald-800', MISSED: 'bg-rose-50 text-rose-800' }
function displayDate(value: string) { return new Intl.DateTimeFormat('en-IN', { dateStyle: 'long' }).format(new Date(`${value}T12:00:00`)) }

export function CareFollowUpsPage() {
  const [items, setItems] = useState<CareFollowUp[]>([]), [loading, setLoading] = useState(true)
  const [updating, setUpdating] = useState(''), [error, setError] = useState('')
  useEffect(() => { if (isStaticDemo) { setLoading(false); return }; getMyFollowUps().then(setItems).catch((cause) => setError(messageFromError(cause))).finally(() => setLoading(false)) }, [])
  async function change(item: CareFollowUp, status: FollowUpStatus) { setUpdating(item.id); setError(''); try { const updated = await updateFollowUpStatus(item.id, status); setItems((current) => current.map((entry) => entry.id === updated.id ? updated : entry)) } catch (cause) { setError(messageFromError(cause)) } finally { setUpdating('') } }
  return <div className="min-h-[70vh] bg-slate-50"><section className="bg-ink-950 text-white"><div className="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:px-8"><p className="text-xs font-black uppercase tracking-[.2em] text-violet-300">Continuity of care</p><h1 className="mt-3 text-4xl font-black tracking-tight">Follow-ups & medication reminders</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-blue-100/75">See doctor-scheduled follow-ups, confirm attendance and open the doctor-issued prescription schedule after discharge.</p></div></section><div className="mx-auto max-w-6xl px-4 py-10 sm:px-6 lg:px-8">{error && <p role="alert" className="mb-5 rounded-2xl bg-rose-50 p-4 text-sm font-bold text-rose-800">{error}</p>}{loading ? <div className="flex min-h-52 items-center justify-center gap-2 font-bold text-slate-500"><LoaderCircle className="size-5 animate-spin" />Loading follow-ups…</div> : items.length === 0 ? <div className="rounded-[2rem] border border-dashed border-slate-300 bg-white p-10 text-center"><CalendarCheck2 className="mx-auto size-10 text-slate-300" /><h2 className="mt-4 text-xl font-black text-ink-950">No follow-up scheduled</h2><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-slate-500">A follow-up appears here only after your treating doctor finalizes a visit and selects a date.</p><Link to="/records" className="mt-5 inline-flex text-sm font-black text-care-700">Open health record →</Link></div> : <div className="grid gap-5 lg:grid-cols-2">{items.map((item) => { const actionableDate = new Date(`${item.followUpDate}T23:59:59`).getTime() <= Date.now(); const terminal = item.status === 'COMPLETED' || item.status === 'MISSED'; return <article key={item.id} className="rounded-[2rem] border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-start justify-between gap-4"><div className="flex gap-3"><span className="grid size-12 shrink-0 place-items-center rounded-2xl bg-violet-50 text-violet-700"><Stethoscope className="size-6" /></span><div><h2 className="font-black text-ink-950">{item.doctorName}</h2><p className="text-sm font-semibold text-care-700">{item.specialization} · {item.hospitalName}</p></div></div><span className={`rounded-full px-3 py-2 text-[11px] font-black ${styles[item.status]}`}>{item.overdue && !terminal ? 'Overdue · ' : ''}{labels[item.status]}</span></div><div className="mt-5 rounded-2xl bg-slate-50 p-4"><p className="flex items-center gap-2 font-black text-ink-950"><CalendarCheck2 className="size-5 text-violet-700" />{displayDate(item.followUpDate)}</p><p className="mt-2 text-xs text-slate-500">Original visit: {displayDate(item.visitDate)}</p>{item.instructions && <p className="mt-3 text-sm font-semibold leading-6 text-slate-700">{item.instructions}</p>}</div>{item.medicationReminderEnabled && <div className="mt-4 flex gap-3 rounded-2xl border border-emerald-200 bg-emerald-50 p-4"><Pill className="mt-0.5 size-5 shrink-0 text-emerald-700" /><div><p className="font-black text-emerald-950">Medication reminder active</p><p className="mt-1 text-xs leading-5 text-emerald-800">Follow only the doctor-issued prescription. Do not change dose, frequency or duration without clinical advice.</p><Link to="/records" className="mt-2 inline-flex text-xs font-black text-emerald-900 underline">Open prescription schedule</Link></div></div>}{!terminal && <div className="mt-5 flex flex-wrap gap-2">{item.status !== 'CONFIRMED' && <button disabled={updating === item.id} onClick={() => void change(item, 'CONFIRMED')} className="rounded-xl bg-care-600 px-4 py-2.5 text-xs font-black text-white disabled:opacity-50">Confirm visit</button>}{actionableDate && <button disabled={updating === item.id} onClick={() => void change(item, 'COMPLETED')} className="rounded-xl border border-emerald-300 px-4 py-2.5 text-xs font-black text-emerald-800 disabled:opacity-50"><CheckCircle2 className="mr-1.5 inline size-4" />Mark completed</button>}{actionableDate && <button disabled={updating === item.id} onClick={() => void change(item, 'MISSED')} className="rounded-xl border border-slate-300 px-4 py-2.5 text-xs font-black text-slate-600 disabled:opacity-50"><Clock3 className="mr-1.5 inline size-4" />Mark missed</button>}</div>}</article> })}</div>}<div className="mt-8 flex gap-3 rounded-3xl bg-amber-50 p-5 text-sm leading-6 text-amber-950"><ShieldCheck className="mt-0.5 size-5 shrink-0" /><p><strong>Reminder only:</strong> SmartCare supports continuity and adherence; it does not prescribe medicine, change a treatment plan or guarantee prevention of readmission.</p></div></div></div>
}
